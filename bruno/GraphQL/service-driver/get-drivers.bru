meta {
  name: get-drivers
  type: graphql
  seq: 1
}

post {
  url: {{api_base}}/graphql
  body: graphql
  auth: inherit
}

body:graphql {
  query {
    drivers {
      items {
        id
        first_name
        last_name
        email
        status
      }
    }
  }
}
